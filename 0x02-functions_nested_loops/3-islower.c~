#include <stdio.h>

int _islower(int c);

/**
* int _islower(int c) - Entry point
* Description: 'Checking numbers out'
* Return: 1 or 0
*/

int _islower(int c)
{
	if (c._islower())
		return (1);
	else
		return (0);
}
